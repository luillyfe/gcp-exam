import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "GCP Cloud Architect Exam",
  description:
    "Building the business case for Professional Cloud Architect training and certification. Diagnose your strengths and weaknesses: Practice problems for the Professional Cloud Architect exam.",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
